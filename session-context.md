# 会话上下文记录

> 此文件供任何智能体在任何终端上共享使用。
> 每次会话启动前：`git pull` → 启动智能体 → 读取本文件 → 获得完整上下文。
> 加载后即可继续之前的对话和工作，无需重新解释。
> 最后更新：YYYY-MM-DD
>
> **自动记录指令（循环闭环）**：
> 
> **每次会话中，当前智能体必须自动完成：**
> 1. 每完成一项工作 → 自动记录到「已完成的工作」
> 2. 用户对工作方法提出修正或确认更好的做法 → 自动记录到「工作方法迭代」
> 3. 每次更新本文件后 → 立即执行 `git backup`（add + commit + push）推送到 GitHub
> 
> **下次会话任何智能体启动时（无论哪台电脑）：**
> 1. 先 `git pull` 拉取最新版 MD
> 2. 读取本文件 → 自动加载用户画像、工作成果、方法偏好、进度
> 3. 继续工作 → 重复上述自动记录和推送流程
> 
> **闭环公式**：做事 → 反馈 → 修正 → 记录 → 推送 → 下次拉取 → 下次做得更好
> 
> **适用对象**：任何智能体在任何终端。无论哪台电脑、哪个智能体，都遵守同一套读写和推送规则。记忆永久循环。

---

## 用户画像

- [在此填写：你是谁、角色、偏好、学习风格、技术环境]

---

## 技术环境

- 系统：[填写]
- 代理：[填写]
- 模型：[填写]
- 工作目录：[填写]

---

## 已完成的工作

[智能体自动记录]

---

## 讨论过的核心概念

[智能体自动记录]

---

## 用户偏好

[智能体自动记录]

---

## 工作方法迭代

> 此部分自动记录用户对工作方法的修正和确认。每次方法论有改进，即时写入。

---

## 书单

### 书单一：[填写名称]

[进度：0/N]

### 书单二：[填写名称]

[进度：0/N]

---

## GitHub 自动备份指令

**每次新会话启动前（任何机器都必须执行）**：
```
cd 你的仓库目录
git pull
```
然后再启动智能体。顺序不能反。

**仓库地址**：https://github.com/你的用户名/你的仓库名（私有）

**Windows**：凭据存入 `~/.git-credentials`，代理通过 `git config --global` 配置
**Mac**：凭据存入 Keychain，代理通过 `git config --global` 配置

**Git 别名 `git backup`**：
```
git add session-context.md && git commit -m "auto-backup" && git -c http.sslVerify=false push
```

---

## 使用说明

将此文件提供给任何智能体。任何智能体读取后即可了解之前的对话、工作成果和用户偏好。
